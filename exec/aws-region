#!/usr/bin/env bash

function main {
  local shome="$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"
  source "$shome/script/profile"

  local nm_region="${0##*/}"
  
  export AWS_PROFILE="${CONTEXT_PREFIX:-}${nm_region}${CONTEXT_SUFFIX:-}" 
  exec aws "$@"
}

source sub "$BASH_SOURCE" "$@"
